{
  "name": "lambda-calendar",
  "version": "0.1.0",
  "bin": {
    "lambda-calendar": "bin/lambda-calendar.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "synth": "cdk synth --profile livanta-deploy",
    "deploy": "cdk deploy --profile livanta-deploy",
    "full-deploy": "tsc && cdk synth --profile livanta-deploy && cdk deploy --profile livanta-deploy"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "aws-cdk": "2.8.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "@aws-sdk/client-connect": "^3.53.0",
    "@aws-sdk/client-dynamodb": "^3.53.0",
    "@aws-sdk/lib-dynamodb": "^3.53.0",
    "aws-cdk-lib": "^2.15.0",
    "aws-sdk": "^2.1088.0",
    "constructs": "^10.0.0",
    "dotenv": "^14.3.2",
    "moment-timezone": "^0.5.34",
    "source-map-support": "^0.5.16"
  }
}
